
import * as types from '../constants/action-types';

const defaultState = {
  displayMain: false
};

export default (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
